矩阵编程———— MMA

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    MMA (Matrix Multiply-Accumulate) 是矩阵乘加操作的缩写，
    指融合了乘法和加法的基础线性代数操作，即 D = A * B + C。
    同时 MMA 行为是矩阵乘 GEMM 运算的核心逻辑，因此在诸多应用中被广泛使用。
    KUPL 基于鲲鹏硬件矩阵计算的能力提供了高效的 MMA 接口，
    通过提供 MMA ops atom + atom shape 的设计方式扩展底层矩阵计算能力从而支持用户快速高效的使能鲲鹏硬件的底层能力进行矩阵GEMM等场景的高效开发

推荐场景
    符合以下特性的问题均可采用 MMA 进行解决
    1、问题涉及矩阵乘行为
    2、问题需要算子融合操作进行细粒度优化

场景示例
    具体以 double 精度的 32 * 16 * 512 矩阵乘为例进行说明：
    针对上述问题 KUPL 可以拆分如下解决步骤：
    1、 KUPL tensor 对象创建
    2、 调用 KUPL mma 接口进行计算
    3、 调用 KUPL store 接口将计算结果写回