众核并行————多队列并行编程

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 的 queue_submit 接口是实现算子间并行的一种编程方法。
    对于增加线程获取的性能收益不明显（线性度不好）的算子，可以采取往队列上提交多个能够并行执行的算子，
    让算子以较高的性能运行，同时充分利用多核的能力。

推荐场景
    1、算子间并行

场景示例
    需要分别计算两个数组 arr1, arr2 中所有元素的和，提交至两个队列实现算子间并行。