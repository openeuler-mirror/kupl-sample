众核并行———— parallel for 1d 并行 for 循环

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 的 parallel for 并行接口是实现多线程编程的一种方法，
    实现类 OMP parallel for 导语能力，多线并行加速 for 循环操作，并提供静态和动态两种策略。
    其中，静态策略会静态决定线程与任务的映射，指定特定线程完成特定任务；
    而动态策略则不做强制映射，有调度器进行调度。
    KUPL 的 parallel for 并行接口通常用来定义算子内并行的计算逻辑，
    可以与上述提到的计算图编程和多队列编程的算子间并行的能力搭配使用。
    此处提供parallel for 1d的示例，实现1维切分。

推荐场景
    符合以下特性的问题均可采用 parallel for 并行
    1、任务逻辑可以通过无依赖循环的形式表达
    2、相比于 OpenMP, KUPL 新增二维、三维的数据切分能力

场景示例
    具体以静态策略下的 C[] = A[] + B[] 为例进行说明：
    针对上述问题，parallel for会对任务做出如下操作：
    1、将提交的任务按照规模拆分成多个子任务；
    2、将子任务分配给对应的线程并行执行