众核并行————计算机图编程

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    静态图即用于描述一个特定复杂问题的依赖关系图（DAG），
    将其中的计算、数据移动等任务定义成静态图中的碘，将各个任务之间的依赖关系定义成静态图中的边。
    KUPL 提供让用户定义静态图的能力，并通过将静态图提交到动态图中执行实现多线程的并行执行与负载均衡调度。

推荐场景
    符合以下特性的问题均可采用计算图进行解决
    1、问题可拆分成多个任务且存在一定依赖关系
    2、问题需要被反复提交执行，如 HPC 应用中的迭代计算等

场景示例
    以计算表达式 E = A * B + C * D 为例进行说明：
    针对上述问题可以拆分如下计算步骤：
    1、计算 F = A * B 和 G = C * D
    2、计算 E = F + G