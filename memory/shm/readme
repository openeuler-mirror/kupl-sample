数据管理————共享内存

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 共享内存管理涉及到两个基本概念：一个是 KUPL comm 通信域，类似 MPI 通信域，
    用于存储进程相关的信息；另一个是 KUPL win，存储一组进程申请的内存地址和进程映射关系。
    一个 KUPL comm 通信域下可以有一个或多个 KUPL win

推荐场景
    推荐在节点内纯 MPI 场景进程间数据搬运时使用。

示例场景
    一共有三个进程，rank 0-2，
    每个进程分别调用 KUPL 共享内存管理中申请内存的接口申请各自申请一块 buffer (编号为 0-2)，
    目标将 rank 0 的数据拷贝到 rank 1 和 rank2。