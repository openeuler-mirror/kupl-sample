数据管理————异步数据拷贝

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 实现了异步数据拷贝，在下发数据拷贝任务后，不必等待拷贝完成。
    异步数据拷贝能够实现数据拷贝和计算的相互隐藏，提高资源利用率。
    KUPL 底层通过 SDMA 设备实现异步数据拷贝

推荐场景
    符合以下特性的问题可采用异步数据拷贝进行解决：
    计算任务与数据拷贝任务不存在数据依赖，希望达到更高的资源利用率

场景示例
    具体将对异步数据拷贝与计算相互隐藏的示例进行说明：
    1、程序需要完成 A->B 的拷贝与 C 的计算
    2、在单核情况下，可通过异步数据拷贝实现任务并行