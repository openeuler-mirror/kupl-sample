数据管理————数据拷贝

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 提供了基于两个虚拟地址的数据拷贝，类似 glibc 的数据拷贝接口。
    System Direct memory access (SDMA) 是系统中负责数据的高带宽低延迟搬运的模块，
    是一种分布式分离式的 DMA 引擎。 KUPL 基于鲲鹏特殊的硬件，实现了数据拷贝的加速。
    KUPL 内部通过阈值设置从而确认不同包大小下 memcpy 行为方式，该阈值大小可通过环境变量进行控制。

推荐场景
    符合以下特性的数据拷贝问题建议使用 KUPL 数据拷贝接口
    用户需要进行高效的数据拷贝