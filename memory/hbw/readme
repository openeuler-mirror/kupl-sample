数据管理————hbw内存管理

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    HBW内存即High Band-Width Memory，对比于Low Band-Width Memory（例如DDR），
    可以实现更高速、更高带宽的内存访问。KUPL提供让用户使用HBW内存的能力，提供HBW内存管理
    相关接口，包括申请、释放内存、验证内存是否属于HBW内存等、设置HBW内存分配策略等功能。

推荐场景
    符合以下特性的问题可采用HBW内存进行解决：
    内存访问成为瓶颈，希望使能更高速、更高带宽的内存访问
    使用场景的目标机器中存在HBW内存相关硬件