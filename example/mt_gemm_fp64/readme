GEMM 算子开发实践

编译命令: make

运行命令: make run

清理命令: make clean

目标算子描述
    算子逻辑：完成 C(64 * 32) = A(64 * 1024) * B(1024 * 32) + C 的矩阵乘计算过程，
    并行场景：实现 4 线程的并行执行，
    通过使能 KUPL 的 SDMA memcpy、计算机图编程和矩阵 MMA 特性进行优化

算子开发要点分析
    由于矩阵乘逻辑的特殊性，可以通过对目标 C 矩阵的切分，实现多线程并行执行
    矩阵乘的操作可以使用矩阵编程指令进行矩阵 kernel 加速

KUPL 改写优化方式
    矩阵 Kernel 加速问题：
        1、GEMM 行为可定义成右图所示依赖关系，通过 KUPL 计算图编程提供的能力，将右图依赖关系多线程加速执行
    矩阵计算多线程问题：
        1、右图中计算任务通过 KUPL 矩阵编程 MMA 加速计算